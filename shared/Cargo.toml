[package]
name = "sizematters-shared"
version.workspace = true
authors.workspace = true
edition.workspace = true

[features]
actix = ["dep:actix"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
md5 = "0.7.0"
actix = { version = "0.13", optional = true }
